{
  "name": "atelier-product-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node scrape.test.js && node universal-scraper.test.js && node server.test.js",
    "check": "node --check server.js && node --check library.js && node --check universal-scraper.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "playwright": "^1.63.0"
  }
}
